% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick_coef_table.R
\name{quick_coef_table}
\alias{quick_coef_table}
\title{Quick Model Coefficient Table}
\usage{
quick_coef_table(
  model,
  digits = 3,
  include_intercept = FALSE,
  sort_by = "none",
  sig_only = FALSE,
  alpha = 0.05
)
}
\arguments{
\item{model}{A fitted model object (glm, lm, or similar) OR a list from cbps_weighted_analysis/npcbps_weighted_analysis}

\item{digits}{Integer. Number of decimal places to round to (default: 3)}

\item{include_intercept}{Logical. Whether to include the intercept term (default: FALSE)}

\item{sort_by}{Character. Sort by "none", "pvalue", "b_value", or "abs_b_value" (default: "none")}

\item{sig_only}{Logical. Show only significant results (default: FALSE)}

\item{alpha}{Numeric. Significance level if sig_only = TRUE (default: 0.05)}
}
\value{
A data frame with columns: variable, b_value, p_value (and optionally sig)
}
\description{
A simple helper function to quickly view coefficients (b-values) and p-values
from a fitted model during interactive analysis. Designed for quick inspection,
not publication-ready tables.
}
\details{
This is a lightweight function for quick model inspection during interactive
analysis. For comprehensive results extraction and publication tables, use
\code{\link{extract_cbps_results}} instead.
}
\examples{
\dontrun{
# Quick look at any model
model <- glm(outcome ~ treatment + age, data = mydata)
quick_coef_table(model)

# From CBPS analysis results
results <- cbps_weighted_analysis(...)
quick_coef_table(results)
quick_coef_table(results$model)  # same thing

# Only significant results, sorted by p-value
quick_coef_table(model, sig_only = TRUE, sort_by = "pvalue")

# Sort by effect size
quick_coef_table(model, sort_by = "abs_b_value", include_intercept = FALSE)
}

}
\seealso{
\code{\link{extract_cbps_results}} for comprehensive results extraction
}
