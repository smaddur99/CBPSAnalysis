% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick_coef_table.R
\name{print_coef_table}
\alias{print_coef_table}
\title{Print Quick Coefficient Table}
\usage{
print_coef_table(
  model,
  digits = 3,
  include_intercept = FALSE,
  sort_by = "none",
  sig_only = FALSE,
  alpha = 0.05
)
}
\arguments{
\item{model}{A fitted model object (glm, lm, or similar) OR a list from cbps_weighted_analysis OR output from extract_cbps_results}

\item{digits}{Integer. Number of decimal places to round to (default: 3)}

\item{include_intercept}{Logical. Whether to include the intercept term (default: FALSE)}

\item{sort_by}{Character. Sort by "none", "pvalue", "b_value", or "abs_b_value" (default: "none")}

\item{sig_only}{Logical. Show only significant results (default: FALSE)}

\item{alpha}{Numeric. Significance level if sig_only = TRUE (default: 0.05)}
}
\value{
Invisibly returns the data frame (mainly used for side effect of printing)
}
\description{
Prints the quick coefficient table with nice formatting to console.
This is a convenience wrapper around \code{quick_coef_table} that prints
the results with a header for easier reading during interactive analysis.
}
\examples{
\dontrun{
results <- cbps_weighted_analysis(...)
print_coef_table(results)
print_coef_table(results, sig_only = TRUE)

# Or with extracted results
extracted <- extract_cbps_results(results)
print_coef_table(extracted)
}

}
