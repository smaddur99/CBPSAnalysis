% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_significance_table.R
\name{create_significance_table}
\alias{create_significance_table}
\title{Create Publication-Ready Significance Table}
\usage{
create_significance_table(
  sig_results,
  table_title = "Treatment Effects Across Model Specifications",
  filename = NULL,
  treatment_label = NULL,
  model_labels = NULL,
  include_bootstrap = TRUE,
  decimal_places = 3,
  font_size = 16,
  font_family = "Times New Roman",
  show_all_models = FALSE,
  alpha_threshold = 0.05
)
}
\arguments{
\item{sig_results}{List or data frame. Output from \code{\link{check_treatment_significance}} or equivalent data frame}

\item{table_title}{Character. Title for the table (default: "Treatment Effects Across Model Specifications")}

\item{filename}{Character. Optional filename to save the table (default: NULL)}

\item{treatment_label}{Character. Label for the treatment variable (default: NULL, uses variable name)}

\item{model_labels}{Named character vector. Pretty names for models (default: NULL)}

\item{include_bootstrap}{Logical. Whether to include bootstrap results (default: TRUE)}

\item{decimal_places}{Integer. Number of decimal places for estimates (default: 3)}

\item{font_size}{Integer. Font size for the table (default: 16)}

\item{font_family}{Character. Font family for the table (default: "Times New Roman")}

\item{show_all_models}{Logical. Whether to show all models or just significant ones (default: FALSE)}

\item{alpha_threshold}{Numeric. Threshold for highlighting significance (default: 0.05)}
}
\value{
A gt table object that can be displayed or saved
}
\description{
Creates a formatted table of treatment effects across model specifications,
suitable for publication or presentation.
}
\details{
This function creates publication-ready tables using the gt package with:
\itemize{
\item Professional formatting suitable for academic papers
\item Significance highlighting (green background for significant results)
\item Confidence intervals for both GLM and bootstrap estimates
\item Balance assessment ratios
\item Sample size information
\item Customizable styling options
}

The function automatically handles:
\itemize{
\item Missing confidence intervals
\item Bootstrap vs. non-bootstrap results
\item Different balance table formats
\item Significance stars (* p < 0.05, ** p < 0.01, *** p < 0.001)
}
}
\examples{
\dontrun{
# After running significance analysis
sig_results <- check_treatment_significance(...)

# Create basic table
table1 <- create_significance_table(
  sig_results = sig_results,
  treatment_label = "Treatment Effect"
)

# Create customized table with all models
table2 <- create_significance_table(
  sig_results = sig_results,
  table_title = "Comparison of Model Specifications",
  model_labels = c("Model_1" = "Basic Model", "Model_2" = "Full Model"),
  show_all_models = TRUE,
  filename = "treatment_table.html"
)
}

}
\seealso{
\code{\link{check_treatment_significance}}, \code{\link{quick_treatment_table}}, \code{\link{full_treatment_table}}
}
